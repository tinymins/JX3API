[English](./Global.md) | 中文

# Global

## 函数

* <h4 id="LuaGetFileCRC">GetFileCRC</h4>
> GetFileCRC(pcszFile) -> dwCRC

计算文件的 CRC32。

* <h4 id="LuaGetStringCRC">GetStringCRC</h4>
> GetStringCRC(pcszText) -> dwCRC

计算字符串的 CRC32。

* <h4 id="LuaEncodeComponentsString">EncodeComponentsString</h4>
> EncodeComponentsString(pcszString) -> pcszEncoded

对 UI 组件字符串进行编码。

* <h4 id="LuaGetPureText">GetPureText</h4>
> GetPureText(pcszString) -> pcszPure

从“组件编码字符串”中提取纯文本。
