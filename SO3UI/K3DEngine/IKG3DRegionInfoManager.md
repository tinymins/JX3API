# IKG3DRegionInfoManager

Methods:
- `GetData`

* <h4 id="LuaRegionInfoMgr_GetData">GetData</h4>
> (number) IKG3DRegionInfoManager:GetData(x, y, z)

Queries region info data at world position `(x, y, z)`.

Returns a number (`dwData`) on success.
